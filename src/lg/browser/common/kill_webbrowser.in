#####################################################################
ps -ef | grep chrome | grep -v grep | awk '{print $2}' | while read pid
do
  echo "====> kill $pid"
    kill -9 $pid
    done
#####################################################################
